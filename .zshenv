# ~/.zshenv: runs for every zsh invocation (login, interactive, scripts).
# All setup lives in ~/.config/shell/env.d/ (sourced by .zshrc) —
# this file is tracked (and kept empty) to prevent tool installers
# from adding PATH/env entries that run on every zsh subprocess.
