#!/usr/bin/env bash
# pre-commit — Git pre-commit hook.
# Delegates to `sley ready --fix --commit` for the full commit-readiness gate.
#
# Last resort: git commit --no-verify

set -u

_hook_dir=$(cd -P -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd) || exit 0
exec "$_hook_dir/sley-commit-gate"
