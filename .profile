# ~/.profile: login shell config for POSIX shells.
# Intentionally empty — bash ignores this when .bash_profile exists,
# and plain sh login shells are not used interactively.
# Tracked to prevent tool installers from adding duplicate PATH/env entries.
