name: Tests

on:
  push:
    branches: [master, main]
  pull_request:
  schedule:
    - cron: "0 7 * * *"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  shell:
    uses: cgraf78/actions/.github/workflows/shell-ci.yml@adf5972f4d55830c0a21e009ecb306264476e205
    with:
      setup: dotfiles
      test-command: .local/bin/dot-test
