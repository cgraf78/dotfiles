name: Tests

on:
  push:
    branches: [master, main]
  pull_request:
  schedule:
    - cron: "0 7 * * *"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  shell:
    uses: cgraf78/actions/.github/workflows/shell-ci.yml@7ed93f1518cb9e0456baeccdc1670063ff277e5f
    with:
      # CI restores shdeps between runs. Refresh the resolver before it
      # validates dependency releases and installed-tool smoke checks.
      force-dotfiles-update: true
      matrix-set: full
      setup: dotfiles
      shellcheck-exclude-codes: SC1091
      shellcheck-inventory-path: .github/shellcheck-files.txt
      test-command: DOT_CORE_SKIP_SHELLCHECK=1 .local/bin/dot-test
      termux-command: |-
        set -euo pipefail
        HOME="$PWD" PATH="$PWD/.local/bin:$PATH" .local/bin/dot update --skip-pull
        HOME="$PWD" PATH="$PWD/.local/bin:$PATH" bash .local/lib/dot/tests/android-ci-smoke
      termux-profiles: base,neovim
