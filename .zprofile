# ~/.zprofile: zsh login shell config.
# Intentionally empty — unlike bash, zsh login shells source both
# .zprofile AND .zshrc, so there's nothing extra to do here.
# Tracked to prevent tool installers from adding duplicate PATH/env entries.
