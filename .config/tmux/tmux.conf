# ~/.config/tmux/tmux.conf

# Load capability fragments in lexical order. Missing optional fragments are
# ignored so the base profile remains independently usable.
source-file -q "$HOME/.config/tmux/conf.d/*.conf"
