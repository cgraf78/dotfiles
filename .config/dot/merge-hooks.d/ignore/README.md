# Ignore Merge Hook

This directory declares the `ignore` merge-hook instance. Its declarative source
family is `ignore.d/` in this directory.

The executable hook implementation lives at
`~/.local/lib/dotfiles/merge-hooks.d/ignore.sh`.
