# Claude Merge Hook

This directory declares the `claude` merge-hook instance. Its declarative source
family is `settings.d/` in this directory.

The executable hook implementation lives at
`~/.local/lib/dot/core/merge-hooks/claude.sh`.
