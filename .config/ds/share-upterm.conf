# Upterm share backend config
# Env vars (DS_UPTERM_*) override these values.
push=openclaw@taylor
server=taylor.web:2222
known-hosts=~/.ssh/known_hosts
authorized-keys=~/.config/ds/upterm_authorized_keys
